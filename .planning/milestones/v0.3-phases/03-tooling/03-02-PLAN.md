---
phase: 03-tooling
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - internal/updater/updater.go
autonomous: true
requirements: [UPDT-02, UPDT-03]

must_haves:
  truths:
    - "FetchLatestVersion returns the tag_name from GitHub Releases API without network in tests"
    - "DownloadAndReplace atomically replaces a file with downloaded content"
    - "AssetName returns correct platform/arch string for the current host"
    - "All three updater tests pass green"
  artifacts:
    - path: "internal/updater/updater.go"
      provides: "Full implementation: FetchLatestVersion, AssetName, DownloadAndReplace"
      exports: ["FetchLatestVersion", "AssetName", "DownloadAndReplace", "ghAPIBase"]
  key_links:
    - from: "internal/updater/updater.go"
      to: "https://api.github.com/repos/{owner}/{repo}/releases/latest"
      via: "net/http GET with Accept + X-GitHub-Api-Version headers"
      pattern: "ghAPIBase.*releases/latest"
    - from: "DownloadAndReplace"
      to: "os.Rename"
      via: "temp file in same dir as target (filepath.Dir(exePath))"
      pattern: "os.CreateTemp.*filepath.Dir"
---

<objective>
Implement the updater package: fetch latest GitHub release version, resolve asset name for current OS/arch, and atomically replace the running binary.

Purpose: Core mechanics of `dtasks update` — network fetch, asset resolution, and safe binary replacement.
Output: internal/updater/updater.go fully implemented; all updater tests pass green.
</objective>

<execution_context>
@/Users/danielmunoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmunoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tooling/03-RESEARCH.md
@.planning/phases/03-tooling/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts from Plan 01 scaffolding. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From internal/updater/updater.go (stub, to be replaced):
```go
package updater

// ghAPIBase is the base URL for GitHub API calls.
// Tests override this to point at a mock server.
var ghAPIBase = "https://api.github.com"

func FetchLatestVersion(repo string) (string, error)
func AssetName() (string, error)
func DownloadAndReplace(url, exePath string) error
```

From internal/updater/updater_test.go (tests to satisfy):
```go
// TestFetchLatestVersion: mock HTTP server at ghAPIBase; expects "v0.3.0"
// TestAssetName: current host; validates "dtasks-{platform}-{arch}[.exe]" pattern
// TestAtomicReplace: temp dir with fake binary; new content written atomically
// TestAtomicReplace_PermissionDenied: read-only dir returns error containing "create temp"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FetchLatestVersion and AssetName</name>
  <files>internal/updater/updater.go</files>
  <behavior>
    - FetchLatestVersion("owner/repo"): GETs `{ghAPIBase}/repos/owner/repo/releases/latest`, sets Accept: application/vnd.github+json and X-GitHub-Api-Version: 2022-11-28 and User-Agent: dtasks-cli headers, decodes `{"tag_name":"..."}`, returns tag string
    - FetchLatestVersion on 4xx/5xx: returns "", fmt.Errorf("github API: status %d", statusCode)
    - AssetName on darwin/arm64: returns "dtasks-macos-arm64", nil
    - AssetName on linux/amd64: returns "dtasks-linux-amd64", nil
    - AssetName on windows/amd64: returns "dtasks-windows-amd64.exe", nil
    - AssetName on unsupported OS (e.g. freebsd): returns "", error containing "unsupported OS"
    - AssetName on unsupported arch: returns "", error containing "unsupported arch"
  </behavior>
  <action>
    Implement FetchLatestVersion in internal/updater/updater.go:
    - Use http.NewRequestWithContext with context.Background()
    - Set three headers: Accept, X-GitHub-Api-Version, User-Agent
    - Decode response body into `struct { TagName string "json:\"tag_name\"" }`
    - Check resp.StatusCode != 200 and return error with status code

    Implement AssetName:
    - Switch on runtime.GOOS: "darwin"->"macos", "linux"->"linux", "windows"->"windows", default->error
    - Switch on runtime.GOARCH: "amd64"->"amd64", "arm64"->"arm64", default->error
    - Format: `dtasks-{platform}-{arch}` with `.exe` suffix if GOOS == "windows"

    Pitfall to avoid: Use `ghAPIBase` (package var) as the base, not a hardcoded URL, so tests can point at httptest.NewServer.

    Run tests after implementing these two functions:
    `go test ./internal/updater/... -run "TestFetchLatestVersion|TestAssetName" -v`
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/Projects/dtasks-cli && go test ./internal/updater/... -run "TestFetchLatestVersion|TestAssetName" -v</automated>
  </verify>
  <done>TestFetchLatestVersion and TestAssetName pass green. No compiler warnings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DownloadAndReplace (atomic binary replacement)</name>
  <files>internal/updater/updater.go</files>
  <behavior>
    - DownloadAndReplace(url, exePath): downloads binary from url, writes to temp file in filepath.Dir(exePath), chmod 0755, os.Rename to exePath — atomic on Unix
    - After successful call: exePath contains the downloaded bytes and is executable
    - On temp file creation failure (read-only dir): returns error wrapping "create temp"
    - On rename failure: cleans up temp file, returns error wrapping "rename"
    - Streams download — does NOT io.ReadAll into memory; uses io.Copy from resp.Body to file
  </behavior>
  <action>
    Implement DownloadAndReplace in internal/updater/updater.go:
    1. `exePath` is the target — use `filepath.Dir(exePath)` as the temp dir (NEVER os.TempDir() — cross-device rename fails)
    2. `os.CreateTemp(dir, ".dtasks-update-*")` — catch error, wrap with "create temp:"
    3. defer cleanup: `defer func() { os.Remove(tmpName) }()` — no-op after successful rename
    4. `io.Copy(tmp, resp.Body)` — stream, do not buffer
    5. `tmp.Close()` before chmod
    6. `os.Chmod(tmpName, 0755)`
    7. `os.Rename(tmpName, exePath)` — wrap error with "rename:"
    8. On Windows rename failure (access denied): attempt rename, if err contains "Access is denied" or similar, return error with message "update: could not replace binary (try running as administrator or use install.sh)"

    Run the full updater test suite:
    `go test ./internal/updater/... -v`
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/Projects/dtasks-cli && go test ./internal/updater/... -v</automated>
  </verify>
  <done>All updater tests pass green (TestFetchLatestVersion, TestAssetName, TestAtomicReplace, TestAtomicReplace_PermissionDenied). go vet ./internal/updater/... clean.</done>
</task>

</tasks>

<verification>
go test ./internal/updater/... -v
go vet ./internal/updater/...
go build ./...
</verification>

<success_criteria>
- All tests in internal/updater pass green
- go vet reports no issues
- Full build passes: go build ./...
</success_criteria>

<output>
After completion, create `.planning/phases/03-tooling/03-02-SUMMARY.md`
</output>
