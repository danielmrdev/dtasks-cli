---
phase: 01-querying
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/repo/repo_test.go
autonomous: true
requirements:
  - FILT-01
  - FILT-02
  - FILT-03
  - FILT-04
  - SORT-01
  - SORT-02
  - SRCH-01
  - SRCH-02
  - SRCH-03

must_haves:
  truths:
    - "Tests for FILT-01 through SORT-02 fail with 'field does not exist' or similar (red phase)"
    - "Tests for SRCH-01 through SRCH-03 fail with 'undefined: repo.TaskSearch' (red phase)"
    - "All new test functions are named per the research test map"
    - "Existing tests continue to pass (no regressions)"
  artifacts:
    - path: "internal/repo/repo_test.go"
      provides: "Test scaffold for all 9 Phase 1 requirements"
      contains: "TestTaskList_FilterToday, TestTaskList_FilterOverdue, TestTaskList_FilterTomorrow, TestTaskList_FilterWeek, TestTaskList_Sort, TestTaskList_SortReverse, TestTaskSearch_Keyword, TestTaskSearch_List, TestTaskSearch_Regex"
  key_links:
    - from: "internal/repo/repo_test.go"
      to: "internal/repo/task.go"
      via: "repo.TaskListOptions fields (Overdue, DueTomorrow, DueWeek, SortBy, Reverse)"
      pattern: "repo\\.TaskListOptions\\{"
    - from: "internal/repo/repo_test.go"
      to: "internal/repo/task.go"
      via: "repo.TaskSearch function call"
      pattern: "repo\\.TaskSearch\\("
---

<objective>
Write failing tests for all 9 Phase 1 requirements before any implementation exists.

Purpose: Establish the RED phase of TDD. Tests define the exact contract for the repo layer — TaskListOptions fields, TaskSearch function signature, and expected behaviors. Implementation plans read these tests to know precisely what to build.
Output: Extended repo_test.go with 9 new test functions, all currently failing.
</objective>

<execution_context>
@/Users/danielmunoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmunoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-querying/01-RESEARCH.md

@internal/repo/repo_test.go
@internal/repo/task.go
@internal/models/models.go
</context>

<feature>
  <name>Phase 1 Querying — test scaffold</name>
  <files>internal/repo/repo_test.go</files>
  <behavior>
    FILT-01 — TestTaskList_FilterToday:
      - Create task with due_date = today → appears in results
      - Create task with due_date = yesterday → appears in results (overdue included)
      - Create task with due_date = tomorrow → does NOT appear
      - Create task with no due_date → does NOT appear

    FILT-02 — TestTaskList_FilterOverdue:
      - Create task with due_date = yesterday → appears
      - Create task with due_date = today → does NOT appear (strictly before today)
      - Create task with no due_date → does NOT appear

    FILT-03 — TestTaskList_FilterTomorrow:
      - Create task with due_date = tomorrow → appears
      - Create task with due_date = today → does NOT appear
      - Create task with due_date = day after tomorrow → does NOT appear

    FILT-04 — TestTaskList_FilterWeek:
      - Create task with due_date = today → appears
      - Create task with due_date = today+6 → appears
      - Create task with due_date = today+7 → does NOT appear
      - Create task with due_date = yesterday → does NOT appear

    SORT-01 — TestTaskList_Sort:
      - Create 3 tasks with different due_dates → SortBy="due" returns them ascending by due_date
      - Create 3 tasks with different created_at (use time.Sleep or explicit Insert order) → SortBy="created" returns oldest first

    SORT-02 — TestTaskList_SortReverse:
      - Create 3 tasks with different due_dates → SortBy="due" + Reverse=true returns them descending

    SRCH-01 — TestTaskSearch_Keyword:
      - Create task with title "Buy groceries" → find "grocer" matches (case-insensitive)
      - Create task with notes "call the doctor" → find "DOCTOR" matches
      - Create task with title "gym" → find "shopping" does NOT match

    SRCH-02 — TestTaskSearch_List:
      - Create tasks in list A and list B → search with ListID=A returns only list A tasks
      - Search without ListID returns tasks from both lists

    SRCH-03 — TestTaskSearch_Regex:
      - Search with Regex=true, keyword "^Buy" matches title starting with "Buy"
      - Search with Regex=true, keyword "[invalid" returns error
      - Search with Regex=true, keyword "(?i)grocery" matches case-insensitively
  </behavior>
  <implementation>
    Add the 9 test functions to internal/repo/repo_test.go after the existing task tests.
    Follow the openTestDB(t) + ListCreate + TaskCreate pattern established in the file.
    Use time.Now() with AddDate for relative dates — same pattern as the codebase.
    Tests will fail to compile until Plan 02 adds the new fields and functions.
    This is expected: commit the failing tests, then proceed to Plan 02.

    For SORT-01/SORT-02: Insert tasks via separate TaskCreate calls (SQLite auto-increments created_at by insertion order in tests). Verify ordering by checking that result IDs match expected sequence.

    For SRCH-03 invalid regex: assert err != nil and tasks == nil.

    Do NOT add any production code in this plan.
  </implementation>
</feature>

<verification>
Run after writing tests (expected: compile error referencing missing fields/functions):
```
cd /Users/danielmunoz/Projects/dtasks-cli && go build ./... 2>&1 | head -20
```

After Plan 02 completes, these commands must pass:
```
go test ./internal/repo/... -run TestTaskList_FilterToday -v
go test ./internal/repo/... -run TestTaskList_FilterOverdue -v
go test ./internal/repo/... -run TestTaskList_FilterTomorrow -v
go test ./internal/repo/... -run TestTaskList_FilterWeek -v
go test ./internal/repo/... -run TestTaskList_Sort -v
go test ./internal/repo/... -run TestTaskList_SortReverse -v
go test ./internal/repo/... -run TestTaskSearch_Keyword -v
go test ./internal/repo/... -run TestTaskSearch_List -v
go test ./internal/repo/... -run TestTaskSearch_Regex -v
```
</verification>

<success_criteria>
- 9 new test functions exist in repo_test.go
- Existing tests (TestListCreate, TestTaskCreate, etc.) still compile and pass
- New tests reference repo.TaskListOptions fields (Overdue, DueTomorrow, DueWeek, SortBy, Reverse) and repo.TaskSearch — these will cause compile errors until Plan 02 adds them
- Commit message: `test(01-querying): add failing tests for filters, sorting, and search`
</success_criteria>

<output>
After completion, create `.planning/phases/01-querying/01-01-SUMMARY.md`
</output>
