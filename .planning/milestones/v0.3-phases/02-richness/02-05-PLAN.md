---
phase: 02-richness
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/task.go
  - internal/repo/task.go
autonomous: true
gap_closure: true
requirements: [MAINT-01, MAINT-02, MAINT-03, MAINT-04, MAINT-05]

must_haves:
  truths:
    - "dtasks rm --completed (no extra args) bulk-deletes all completed tasks after confirmation"
    - "dtasks rm --completed --dry-run shows count and task list without deleting or prompting"
    - "dtasks rm --completed --yes skips confirmation and deletes immediately"
    - "dtasks rm --completed --list <id> scopes bulk delete to tasks in a specific list"
    - "go test ./... passes GREEN"
  artifacts:
    - path: "cmd/task.go"
      provides: "--completed as BoolVar; rmCmd bulk path reads rmCompleted bool"
    - path: "internal/repo/task.go"
      provides: "TaskDeleteCompleted works with empty Before string (no date cutoff)"
  key_links:
    - from: "cmd/task.go rmCmd"
      to: "repo.TaskDeleteCompleted"
      via: "DeleteCompletedOptions{Before: \"\", DryRun: true/false}"
      pattern: "TaskDeleteCompleted"
---

<objective>
Fix the --completed flag type in rmCmd so bulk-delete works correctly.

Purpose: UAT tests 6/7/8 all fail because --completed is declared as StringVar. Cobra consumes subsequent flags (--dry-run, --yes, --list) as the value of --completed, breaking all bulk-delete paths.
Output: cmd/task.go with --completed as BoolVar; repo.TaskDeleteCompleted updated to handle empty Before (no date cutoff).
</objective>

<execution_context>
@/Users/danielmunoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmunoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From cmd/task.go — current broken declarations to replace -->
```go
// BROKEN — to replace:
var rmCompleted string  // line 316
// init():
rmCmd.Flags().StringVar(&rmCompleted, "completed", "", "Bulk-delete completed tasks on or before YYYY-MM-DD")

// FIXED — target state:
var rmCompleted bool
// init():
rmCmd.Flags().BoolVar(&rmCompleted, "completed", false, "Bulk-delete all completed tasks")
```

<!-- From internal/repo/task.go — DeleteCompletedOptions and TaskDeleteCompleted -->
```go
type DeleteCompletedOptions struct {
    Before string   // YYYY-MM-DD cutoff; empty string = no date filter (delete all completed)
    ListID *int64
    DryRun bool
}

// Current WHERE clause (line 363):
where := ` WHERE t.completed = 1 AND date(t.completed_at) <= ?`
args := []any{opts.Before}

// Required behaviour: when opts.Before == "", omit the date filter entirely
```

<!-- From internal/repo/repo_test.go — existing tests that MUST keep passing -->
// TestTaskDeleteCompleted (MAINT-01): calls with Before: "2026-03-01" — must still work
// TestTaskDeleteCompleted_DryRun (MAINT-02): calls with Before: "2026-03-01", DryRun: true — must still work
// TestTaskDeleteCompleted_Scoped (MAINT-05): calls with ListID + Before date — must still work
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix TaskDeleteCompleted to support empty Before (no date cutoff)</name>
  <files>internal/repo/task.go, internal/repo/repo_test.go</files>
  <behavior>
    - When Before == "": SELECT and DELETE all completed tasks (no date filter)
    - When Before != "": SELECT and DELETE completed tasks WHERE date(completed_at) <= Before (existing behavior preserved)
    - ListID filter always applied when non-nil
    - DryRun=true: returns Tasks slice, Deleted=0, no DELETE executed
    - DryRun=false: executes DELETE, returns Deleted count

    Test cases to add in repo_test.go (comment: TestTaskDeleteCompleted_NoBefore — MAINT-04):
    - Create 2 completed tasks in a list; call TaskDeleteCompleted with Before="" and DryRun:false
    - Expect result.Deleted == 2 and remaining completed tasks == 0
    - Call TaskDeleteCompleted with Before="" and DryRun:true on fresh tasks
    - Expect result.Deleted == 0 and len(result.Tasks) == 2 (no actual delete)
  </behavior>
  <action>
    In internal/repo/task.go, function TaskDeleteCompleted:

    Replace the hardcoded WHERE clause:
      where := ` WHERE t.completed = 1 AND date(t.completed_at) <= ?`
      args := []any{opts.Before}

    With conditional logic:
      var where string
      var args []any
      if opts.Before != "" {
          where = ` WHERE t.completed = 1 AND date(t.completed_at) <= ?`
          args = []any{opts.Before}
      } else {
          where = ` WHERE t.completed = 1`
          args = []any{}
      }

    Apply the same split to the DELETE branch (delWhere/delArgs):
      var delWhere string
      var delArgs []any
      if opts.Before != "" {
          delWhere = ` WHERE completed = 1 AND date(completed_at) <= ?`
          delArgs = []any{opts.Before}
      } else {
          delWhere = ` WHERE completed = 1`
          delArgs = []any{}
      }
    Apply ListID filter to both branches after the if/else (existing logic, unchanged).

    In internal/repo/repo_test.go, add TestTaskDeleteCompleted_NoBefore after the existing MAINT-05 test:
      - Create list + 2 tasks, mark both completed via direct SQL UPDATE
      - Call TaskDeleteCompleted with DeleteCompletedOptions{} (Before="", DryRun:false)
      - Assert result.Deleted == 2
      - Verify tasks no longer exist via TaskList
      - Add a second sub-test for DryRun:true variant asserting Deleted==0 and len(Tasks)==2
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/Projects/dtasks-cli && go test ./internal/repo/... -run TestTaskDeleteCompleted -v</automated>
  </verify>
  <done>All TestTaskDeleteCompleted* tests pass GREEN including new TestTaskDeleteCompleted_NoBefore</done>
</task>

<task type="auto">
  <name>Task 2: Change --completed to BoolVar in rmCmd</name>
  <files>cmd/task.go</files>
  <action>
    In cmd/task.go:

    1. Change the var declaration (line ~316):
       FROM: var rmCompleted string
       TO:   var rmCompleted bool

    2. In init() (line ~399), replace flag registration:
       FROM: rmCmd.Flags().StringVar(&rmCompleted, "completed", "", "Bulk-delete completed tasks on or before YYYY-MM-DD")
       TO:   rmCmd.Flags().BoolVar(&rmCompleted, "completed", false, "Bulk-delete all completed tasks")

    3. In rmCmd RunE, the bulk path condition `cmd.Flags().Changed("completed")` stays unchanged — it still guards the bulk path correctly.

    4. Update opts construction — remove the Before field assignment since there is no date cutoff:
       FROM:
         opts := repo.DeleteCompletedOptions{
             Before: rmCompleted,   // rmCompleted was string
             DryRun: true,
         }
       TO:
         opts := repo.DeleteCompletedOptions{
             DryRun: true,
         }
       (Before defaults to "" which now means "no date filter" per Task 1)

    5. The second call (DryRun:false path) already sets opts.DryRun = false — no change needed there.

    Do NOT change any other flag, command, or logic. The TTY check, --yes path, --dry-run path, --list scoping, and PrintDeletedCount call are all correct and must remain untouched.
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/Projects/dtasks-cli && go build ./... && go test ./...</automated>
  </verify>
  <done>Binary builds cleanly; go test ./... GREEN; dtasks rm --completed --dry-run no longer triggers "flag needs an argument" error</done>
</task>

</tasks>

<verification>
After both tasks:
1. `go test ./...` passes with no failures
2. `make build` produces dist/dtasks without errors
3. Manual smoke:
   - `dist/dtasks task rm --completed --dry-run` → shows count of completed tasks or "No tasks would be deleted." (no prompt, no delete)
   - `dist/dtasks task rm --completed --yes` → deletes all completed tasks without prompt
   - `dist/dtasks task rm --completed --list <id>` → deletes only completed tasks in that list
</verification>

<success_criteria>
- `--completed` is a boolean flag: `dtasks rm --completed` works without requiring a date argument
- `--dry-run` and `--yes` parse correctly because Cobra no longer consumes them as the value of `--completed`
- `--list` scoping works for the same reason
- All existing repo tests (MAINT-01, MAINT-02, MAINT-05) remain GREEN
- New TestTaskDeleteCompleted_NoBefore covers the "delete all completed" case (MAINT-04 coverage)
- `go test ./...` GREEN; `go build ./...` clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-richness/02-richness-05-SUMMARY.md`
</output>