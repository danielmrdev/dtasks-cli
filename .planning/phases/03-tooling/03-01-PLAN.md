---
phase: 03-tooling
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/updater/updater.go
  - internal/updater/updater_test.go
  - internal/skill/skill.go
  - internal/skill/skill_test.go
  - cmd/update_test.go
autonomous: true
requirements: [UPDT-01, UPDT-02, UPDT-03, UPDT-04, SKIL-01, SKIL-02, SKIL-03, SKIL-04]

must_haves:
  truths:
    - "Test files compile and run (fail red) without network access"
    - "Mock HTTP server is available for updater tests"
    - "Stub functions exist in updater.go and skill.go so tests can import them"
  artifacts:
    - path: "internal/updater/updater.go"
      provides: "Stub exports: FetchLatestVersion, AssetName, DownloadAndReplace"
      exports: ["FetchLatestVersion", "AssetName", "DownloadAndReplace"]
    - path: "internal/updater/updater_test.go"
      provides: "Failing tests for UPDT-02 and UPDT-03"
      contains: "TestFetchLatestVersion, TestAtomicReplace"
    - path: "internal/skill/skill.go"
      provides: "Stub exports: DetectClaude, InstallSkill"
      exports: ["DetectClaude", "InstallSkill"]
    - path: "internal/skill/skill_test.go"
      provides: "Failing tests for SKIL-01..04"
      contains: "TestDetectClaude, TestInstallSkill_NonTTY, TestInstallSkill_Path, TestInstallSkill_Overwrite"
    - path: "cmd/update_test.go"
      provides: "Failing test for UPDT-01 and UPDT-04"
      contains: "TestUpdateCmd_JSON"
  key_links:
    - from: "internal/updater/updater_test.go"
      to: "internal/updater/updater.go"
      via: "package updater imports"
      pattern: "package updater"
    - from: "internal/skill/skill_test.go"
      to: "internal/skill/skill.go"
      via: "package skill imports"
      pattern: "package skill"
---

<objective>
Create failing test scaffolds (TDD red phase) and stub implementations for the updater and skill packages.

Purpose: Establish the test contracts before implementation so Wave 1 plans implement against known expectations.
Output: Four files — two stub packages (updater, skill) and two test files — plus cmd/update_test.go. All tests fail red with "not implemented" panics or zero-value returns.
</objective>

<execution_context>
@/Users/danielmunoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmunoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tooling/03-RESEARCH.md
@.planning/phases/03-tooling/03-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold internal/updater package (stub + failing tests)</name>
  <files>internal/updater/updater.go, internal/updater/updater_test.go</files>
  <behavior>
    - TestFetchLatestVersion: mock HTTP server returns `{"tag_name":"v0.3.0"}`; FetchLatestVersion("owner/repo") returns "v0.3.0", nil
    - TestFetchLatestVersion_NetworkError: mock server returns 500; function returns "", non-nil error
    - TestAssetName: on darwin/arm64 returns "dtasks-macos-arm64"; on linux/amd64 returns "dtasks-linux-amd64"; on windows/amd64 returns "dtasks-windows-amd64.exe"
    - TestAtomicReplace: creates a temp dir with a fake binary, writes new content to it atomically; after call the file contains the new content and is executable (mode &0111 != 0)
    - TestAtomicReplace_PermissionDenied: if target dir is not writable, returns error containing "create temp"
  </behavior>
  <action>
    Create internal/updater/updater.go with package updater and stub signatures that return errors or zero values:
    - `FetchLatestVersion(repo string) (string, error)` — returns "", fmt.Errorf("not implemented")
    - `AssetName() (string, error)` — returns "", fmt.Errorf("not implemented")
    - `DownloadAndReplace(url, exePath string) error` — returns fmt.Errorf("not implemented")

    Create internal/updater/updater_test.go with a mock HTTP server (httptest.NewServer) that returns a fixed JSON body. Tests call the stub functions and assert the errors; tests MUST fail red (functions return "not implemented"). Structure:
    - `newMockGHAPI(t, tagName string) *httptest.Server` helper that returns JSON `{"tag_name":"<tagName>"}`
    - TestFetchLatestVersion uses the mock server URL as the base URL (FetchLatestVersion must accept a base URL override for testing — add an unexported var `ghAPIBase = "https://api.github.com"` that tests override)
    - TestAssetName: use runtime.GOOS/GOARCH, so this test is inherently platform-specific; just verify the returned string matches the expected pattern for the current host
    - TestAtomicReplace: use t.TempDir(), write a file there, call atomicReplace (unexported helper, test via exported DownloadAndReplace with a mock HTTP server serving raw bytes)

    Note: DownloadAndReplace streams the download to disk — do NOT io.ReadAll into memory.
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/Projects/dtasks-cli && go test ./internal/updater/... -v 2>&1 | grep -E "FAIL|PASS|not implemented"</automated>
  </verify>
  <done>Tests compile and fail red (not implemented errors or assertion failures). No compilation errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold internal/skill package (stub + failing tests)</name>
  <files>internal/skill/skill.go, internal/skill/skill_test.go</files>
  <behavior>
    - TestDetectClaude_FoundDotClaude: temp home dir with `.claude/` subdir; DetectClaude(home) returns true
    - TestDetectClaude_FoundConfigClaude: temp home dir with `.config/claude/` subdir; DetectClaude(home) returns true
    - TestDetectClaude_NotFound: empty temp home dir and claude not in PATH; DetectClaude(home) returns false
    - TestInstallSkill_Path: installs skill content to `<tmpHome>/.claude/skills/dtasks-cli/SKILL.md`; file exists after call
    - TestInstallSkill_Overwrite: call InstallSkill twice with different content; second content wins (silent overwrite)
    - TestInstallSkill_NonTTY: when isatty is false (simulate by passing an io.Reader that is a bytes.Buffer), InstallSkill returns nil without prompting (i.e., skill is still installed — non-TTY means skip the prompt but proceed with install if called programmatically)
  </behavior>
  <action>
    Create internal/skill/skill.go with package skill and stubs:
    - `DetectClaude(homeDir string) bool` — returns false (stub)
    - `InstallSkill(homeDir string, content []byte) error` — returns fmt.Errorf("not implemented") (stub)
    - `PromptAndInstall(homeDir string, content []byte, in io.Reader, out io.Writer) error` — returns fmt.Errorf("not implemented") (stub)

    DetectClaude takes homeDir as a parameter (not os.UserHomeDir()) to keep it testable.
    InstallSkill always overwrites: `os.MkdirAll` + `os.WriteFile` — used for programmatic install (update path).
    PromptAndInstall checks if `in` is a real TTY (`term.IsTerminal`) — if not, calls InstallSkill directly without prompt; if yes, prompts "Install dtasks skill for Claude Code? [y/N] " and installs on y.

    Create internal/skill/skill_test.go with tests that use t.TempDir() for homeDir. Tests fail red because stubs return errors.

    Import note: use `golang.org/x/term` for TTY detection — add to go.mod if not present. Check go.mod first with `cat go.mod`; if not present, run `GOPROXY=https://goproxy.io,direct go get golang.org/x/term`.
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/Projects/dtasks-cli && go test ./internal/skill/... -v 2>&1 | grep -E "FAIL|PASS|not implemented"</automated>
  </verify>
  <done>Tests compile and fail red. No compilation errors. golang.org/x/term added to go.mod if needed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold cmd/update_test.go (stub + failing test)</name>
  <files>cmd/update_test.go</files>
  <behavior>
    - TestUpdateCmd_JSON: executes the update subcommand with --json flag using cobra's Execute on a test command; asserts output contains `"current"` and `"latest"` JSON keys; test fails because updateCmd does not exist yet
    - TestUpdateCmd_Help: `dtasks update --help` exits 0 and contains "Check for" in output
  </behavior>
  <action>
    Create cmd/update_test.go in package cmd. Since updateCmd doesn't exist yet, the test file should import the cmd package and reference `updateCmd` by name. This will cause a compile error until update.go is created — that is intentional (red phase for the cmd layer).

    Structure the test to use `rootCmd.SetArgs([]string{"update", "--json"})` and capture stdout via `rootCmd.SetOut`. Mock the network by setting a package-level `ghAPIBase` variable (the same one used in updater package) to a test server URL.

    If referencing `updateCmd` directly causes issues before the file exists, write a build tag `//go:build ignore` initially — then remove it in Plan 04 when updateCmd is created. Document this in a comment.

    Alternative approach (cleaner): write cmd/update_test.go but guard with a compile check using a function that references `updateCmd` — this will fail to compile until Plan 04 creates the command, making the red phase explicit.
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/Projects/dtasks-cli && go build ./cmd/... 2>&1 | head -5; echo "exit: $?"</automated>
  </verify>
  <done>cmd/update_test.go exists. The build may fail (expected — updateCmd missing) or pass if using build tags. Document the expected state in a comment at the top of the file.</done>
</task>

</tasks>

<verification>
go test ./internal/updater/... ./internal/skill/... -v
go vet ./internal/updater/... ./internal/skill/...
</verification>

<success_criteria>
- internal/updater/ compiles, tests run and fail red
- internal/skill/ compiles, tests run and fail red
- cmd/update_test.go exists and references the intended test cases
- go vet reports no issues on the two new packages
</success_criteria>

<output>
After completion, create `.planning/phases/03-tooling/03-01-SUMMARY.md`
</output>
