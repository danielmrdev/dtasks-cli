---
phase: 02-richness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/repo/repo_test.go
  - internal/output/output_test.go
autonomous: true
requirements:
  - PRIO-01
  - PRIO-02
  - PRIO-03
  - PRIO-04
  - MAINT-01
  - MAINT-02
  - MAINT-05
  - STAT-01
  - STAT-02

must_haves:
  truths:
    - "All new tests exist and compile (even if they call functions not yet implemented)"
    - "Running go test ./internal/repo/... fails with 'undefined: repo.X' or type mismatch — not 'PASS'"
    - "Running go test ./internal/output/... fails with 'undefined: output.PrintStats' or similar"
  artifacts:
    - path: "internal/repo/repo_test.go"
      provides: "Failing tests for PRIO-01, PRIO-02, PRIO-04, MAINT-01, MAINT-02, MAINT-05, STAT-01"
      contains: "TestTaskCreate_WithPriority"
    - path: "internal/output/output_test.go"
      provides: "Failing tests for PRIO-03, STAT-02"
      contains: "TestPrintTasks_Priority"
  key_links:
    - from: "internal/repo/repo_test.go"
      to: "repo.TaskDeleteCompleted"
      via: "direct call — must fail to compile until Plan 02 adds the function"
      pattern: "TaskDeleteCompleted"
    - from: "internal/output/output_test.go"
      to: "output.PrintStats"
      via: "direct call — must fail to compile until Plan 03 adds the function"
      pattern: "PrintStats"
---

<objective>
Write all failing tests for Phase 2 requirements before any implementation exists (TDD red phase).

Purpose: Establish the test contracts that Plans 02-04 will satisfy. Tests compile but fail because the repo/output functions they call do not yet exist.
Output: Extended repo_test.go and output_test.go with new failing test stubs.
</objective>

<execution_context>
@/Users/danielmunoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmunoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-richness/02-RESEARCH.md

@internal/repo/repo_test.go
@internal/output/output_test.go
</context>

<interfaces>
<!-- openTestDB pattern from internal/repo/repo_test.go -->
```go
func openTestDB(t *testing.T) *sql.DB {
    f, _ := os.CreateTemp("", "dtasks-repo-test-*.db")
    f.Close()
    t.Cleanup(func() { os.Remove(f.Name()) })
    d, _ := db.Open(f.Name())
    t.Cleanup(func() { d.Close() })
    return d
}
```

<!-- Functions that DO NOT exist yet (tests must call them to fail RED) -->
// repo.TaskDeleteCompleted(db *sql.DB, opts DeleteCompletedOptions) (DeleteCompletedResult, error)
// repo.TaskStats(db *sql.DB) (*StatsSummary, error)
// output.PrintStats(s *repo.StatsSummary)

<!-- models.Task does NOT have Priority field yet — tests will use TaskInput.Priority which also doesn't exist -->
// repo.TaskInput.Priority *string  — does not exist yet
// repo.TaskPatch.Priority *string  — does not exist yet
// repo.TaskListOptions.SortBy accepts "priority" — does not exist in sortMap yet
</interfaces>

<feature>
  <name>TDD red phase — all Phase 2 failing tests</name>
  <files>internal/repo/repo_test.go, internal/output/output_test.go</files>

  <behavior>
    REPO TESTS (append to internal/repo/repo_test.go):

    TestTaskCreate_WithPriority — PRIO-01:
    - Create task with Priority = ptr("high")
    - Retrieve with TaskGet; assert t.Priority != nil && *t.Priority == "high"
    - Create task with nil Priority; assert t.Priority == nil

    TestTaskPatchFields_Priority — PRIO-02:
    - Create task (no priority); patch with Priority = ptr("medium")
    - Assert retrieved task has *t.Priority == "medium"
    - Patch again with Priority = ptr("") to clear; assert t.Priority == nil

    TestTaskList_SortPriority — PRIO-04:
    - Create 3 tasks in list: priority high, priority low, priority medium (insert in that order)
    - Call TaskList with SortBy="priority"; assert order is high → medium → low
    - Create 4th task with nil priority; assert it sorts last

    TestTaskDeleteCompleted — MAINT-01:
    - Create 2 tasks; mark both done (set completed=1 via TaskDone); set completed_at to a known date via SQL
    - Call TaskDeleteCompleted(db, DeleteCompletedOptions{Before: "2026-03-01"})
    - Assert Deleted == 2; assert tasks no longer exist in DB

    TestTaskDeleteCompleted_DryRun — MAINT-02:
    - Same setup as above
    - Call with DryRun: true
    - Assert Deleted == 0; assert Tasks slice has len 2; assert tasks STILL exist in DB

    TestTaskDeleteCompleted_Scoped — MAINT-05:
    - Create 2 lists; add 1 completed task to each; mark both done
    - Call TaskDeleteCompleted with ListID = &list1.ID, Before = today
    - Assert only 1 task deleted (from list1); list2 task still exists

    TestTaskStats — STAT-01:
    - Create 2 lists; add 2 pending + 1 done to list1; add 1 pending to list2
    - Call TaskStats(db)
    - Assert summary.Total == 4, summary.Pending == 3, summary.Done == 1
    - Assert len(summary.ByList) == 2
    - Assert list1 entry: Total=3, Pending=2, Done=1
    - Assert list2 entry: Total=1, Pending=1, Done=0
    - Create empty list3 (no tasks); assert it appears with Total=0

    OUTPUT TESTS (append to internal/output/output_test.go):

    TestPrintTasks_Priority — PRIO-03:
    - Build []models.Task with one task having Priority = ptr("high"), another ptr("medium"), another nil
    - Capture stdout (redirecting os.Stdout to a pipe or bytes.Buffer via captureOutput helper)
    - Call PrintTasks(tasks)
    - Assert output contains "!" for high, "~" for medium, " " (space) for nil
    - Assert output contains "PRIO" header

    TestPrintStats_Table — STAT-02 (table mode):
    - Build StatsSummary with 2 ByList entries
    - Call PrintStats(&summary) with JSONMode = false
    - Assert output contains list names, Total/Pending/Done columns

    TestPrintStats_JSON — STAT-02 (JSON mode):
    - Set output.JSONMode = true; defer reset
    - Call PrintStats(&summary)
    - Assert output is valid JSON containing "total", "pending", "done", "by_list"
  </behavior>

  <implementation>
    RED phase only. Do NOT implement any production code. Write tests that:
    1. Import the packages (repo, output, models) as they will be after implementation
    2. Call functions that do not yet exist (TaskDeleteCompleted, TaskStats, PrintStats)
    3. Reference fields that do not yet exist (TaskInput.Priority, TaskPatch.Priority, models.Task.Priority)

    The tests MUST fail to compile or fail at runtime. Confirm failure before committing.

    Helper needed in repo_test.go:
    ```go
    func strPtr(s string) *string { return &s }
    func int64Ptr(i int64) *int64 { return &i }
    ```

    For TestTaskDeleteCompleted* tests, completed_at must be set to a known date. Use direct SQL:
    ```go
    db.Exec(`UPDATE tasks SET completed = 1, completed_at = ? WHERE id = ?`, "2026-02-15T10:00:00Z", id)
    ```
    (Do not call TaskDone — it uses datetime('now') which can't be controlled in tests.)

    For output tests, capture stdout using this pattern (already exists in output_test.go — reuse):
    Check if captureOutput or equivalent helper exists; if not, use os.Pipe() directly.

    Commit message after RED phase confirmed:
    test(02-01): add failing tests for priority, bulk-delete, stats (TDD red)
  </implementation>
</feature>

<verification>
Run after adding tests:

```bash
cd /Users/danielmunoz/Projects/dtasks-cli && go test ./internal/repo/... 2>&1 | head -20
cd /Users/danielmunoz/Projects/dtasks-cli && go test ./internal/output/... 2>&1 | head -20
```

Expected: compile errors (undefined: repo.TaskDeleteCompleted, etc.) OR test failures — NOT "PASS".
If any new test passes unexpectedly, the test is wrong — fix it before committing.
</verification>

<success_criteria>
- internal/repo/repo_test.go contains all 7 new test functions
- internal/output/output_test.go contains TestPrintTasks_Priority, TestPrintStats_Table, TestPrintStats_JSON
- go test ./internal/... fails (compile error or test failure) — confirms RED state
- No production code changed
</success_criteria>

<output>
After completion, create `.planning/phases/02-richness/02-01-SUMMARY.md`
</output>
