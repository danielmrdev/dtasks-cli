---
phase: 05-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [cmd/task.go]
autonomous: true
requirements: [SORT-01, PRIO-04]

must_haves:
  truths:
    - "dtasks task ls --help shows 'priority' in the --sort flag description"
    - "Shell completion for --sort still returns 'due', 'created', 'completed', 'priority'"
    - "All existing tests pass without modification"
  artifacts:
    - path: "cmd/task.go"
      provides: "Updated --sort flag usage string"
      contains: "Sort by: due, created, completed, priority"
  key_links:
    - from: "cmd/task.go line 149"
      to: "--help output"
      via: "Cobra flag usage string"
      pattern: "Sort by: due, created, completed, priority"
---

<objective>
Fix the --sort flag help text in lsCmd to advertise "priority" as a valid sort value.

Purpose: The repo layer and shell completion already support --sort=priority (TestTaskList_SortPriority passes; RegisterFlagCompletionFunc returns "priority"). The only gap is the usage string shown in --help, which currently reads "Sort by: due, created, completed" and omits "priority".
Output: cmd/task.go with the corrected string; all tests green.
</objective>

<execution_context>
@/Users/danielmunoz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmunoz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix --sort flag usage string</name>
  <files>cmd/task.go</files>
  <action>
    On line 149 of cmd/task.go, change the StringVar usage string from:
      "Sort by: due, created, completed"
    to:
      "Sort by: due, created, completed, priority"

    Do NOT touch any other line. Specifically:
    - Do NOT modify RegisterFlagCompletionFunc on lines 152-154 — it already returns the correct values.
    - Do NOT modify any repo layer code — TestTaskList_SortPriority already passes.
    - This is a single string literal edit; no other files need changes.
  </action>
  <verify>
    <automated>cd /Users/danielmunoz/Projects/dtasks-cli && go build ./... && go test ./... -count=1 && ./dist/dtasks task ls --help | grep -q "priority" && echo "PASS: priority in help text"</automated>
  </verify>
  <done>
    - `dtasks task ls --help` output contains "priority" in the --sort description
    - `go test ./...` passes with no failures
    - Shell completion function unchanged (still returns all four values including "priority")
  </done>
</task>

</tasks>

<verification>
Full suite: `go test ./... -count=1` — all packages green.
Smoke: `./dist/dtasks task ls --help | grep "Sort by:"` — output shows "Sort by: due, created, completed, priority".
</verification>

<success_criteria>
- `dtasks task ls --help` shows "Sort by: due, created, completed, priority"
- `go test ./... -count=1` exits 0 with no failures
- No diff in any file other than cmd/task.go line 149
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-01-SUMMARY.md`
</output>
